---
layout: base.njk
title: Tamburello360 - La Casa Indipendente del Tamburello Italiano
---

{# 1. HERO SECTION: EMOZIONE E UTILITÀ #}
<section class="hero-magazine">
  <div class="hero-overlay"></div>
  <div class="hero-content container">
    <div class="hero-text">
      <span class="badge-indipendente">Progetto Indipendente</span>
      <h1>Il battito della <span class="highlight">Terra Rossa</span></h1>
      <p>Non siamo la federazione. Siamo la community. Risultati, storie e passione per lo sport più autentico d'Italia.</p>
      <div class="hero-actions">
        <a href="/partite/" class="btn btn-primary">Match Center</a>
        <a href="#progetto" class="btn btn-outline">Scopri la Mission</a>
      </div>
    </div>
    
    {# Mini Widget Prossima Partita #}
    {% set nextMatch = collections.partiteEnriched | selectattr("is_recommended", "equalto", true) | first %}
    {% if nextMatch %}
    <div class="hero-widget">
      <h3>In Evidenza</h3>
      <div class="match-mini-card">
        <div class="match-teams">
          <span>{{ nextMatch.home_team.name }}</span>
          <span class="vs">vs</span>
          <span>{{ nextMatch.away_team.name }}</span>
        </div>
        <div class="match-info">
          {{ nextMatch.date_day_label }} ore {{ nextMatch.date_time_label }}
        </div>
        <a href="{{ nextMatch.info_url }}" class="link-overlay">Info Match</a>
      </div>
    </div>
    {% endif %}
  </div>
</section>

{# 2. RISULTATI RECENTI: IL TICKER DINAMICO #}
<div class="results-ticker">
  <div class="container flex-ticker">
    <span class="ticker-label">Ultimi Risultati:</span>
    <div class="ticker-items">
      {% for match in collections.partiteRecenti | limit(5) %}
        <div class="ticker-item">
          <strong>{{ match.squadra_casa }}</strong> {{ match.gol_casa }} - {{ match.gol_ospite }} <strong>{{ match.squadra_ospite }}</strong>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{# 3. MAGAZINE SECTION: LOOK EDITORIALE #}
<section class="magazine-grid container section-padding">
  <div class="section-header">
    <h2>Storie di <span class="highlight">Campo</span></h2>
    <a href="/blog/" class="view-all">Leggi tutto il magazine →</a>
  </div>

  <div class="magazine-layout">
    {% set posts = collections.blogVisible %}
    {% if posts.length > 0 %}
      {# Articolo Principale #}
      <article class="post-featured">
        <div class="post-img-wrapper">
          <img src="{{ posts[0].data.image }}" alt="{{ posts[0].data.title }}">
        </div>
        <div class="post-content">
          <span class="post-category">{{ posts[0].data.category }}</span>
          <h3><a href="{{ posts[0].url }}">{{ posts[0].data.title }}</a></h3>
          <p>{{ posts[0].data.excerpt | truncate(150) }}</p>
          <time>{{ posts[0].data.date | readableDate }}</time>
        </div>
      </article>

      {# Articoli Secondari #}
      <div class="post-sidebar">
        {% for post in posts | slice(1, 3) %}
          <article class="post-mini">
            <img src="{{ post.data.image }}" alt="{{ post.data.title }}">
            <div>
              <h4><a href="{{ post.url }}">{{ post.data.title }}</a></h4>
              <time>{{ post.data.date | readableDate }}</time>
            </div>
          </article>
        {% endfor %}
      </div>
    {% endif %}
  </div>
</section>

{# 4. DATA & MISSION: CONTRASTO VISIVO #}
<section class="split-section">
  <div class="container split-grid">
    {# Classifica Rapida #}
    <div class="standings-snapshot">
      <h3>Classifica Serie A <span class="year">2025</span></h3>
      {% set classificaA = collections.classificheSerieA[0] %}
      {% if classificaA %}
        <table class="mini-table">
          {% for team in classificaA.data.teams | limit(5) %}
          <tr>
            <td>{{ team.position }}°</td>
            <td class="team-name">{{ team.name }}</td>
            <td class="points">{{ team.points }}</td>
          </tr>
          {% endfor %}
        </table>
        <a href="/classifiche/" class="btn-text">Vedi classifica completa</a>
      {% endif %}
    </div>

    {# Mission del Progetto #}
    <div id="progetto" class="mission-box">
      <div class="mission-tag">Chi Siamo</div>
      <h3>Non è solo un sito. È un <span class="highlight">omaggio</span>.</h3>
      <p>Tamburello360 nasce dal desiderio di non lasciar scomparire la narrazione di uno sport che è storia, polvere e talento. Gestiamo i dati in modo automatico per lasciar spazio a ciò che conta: il racconto delle persone e dei territori.</p>
      <div class="signature">Creato con ❤️ da un appassionato</div>
    </div>
  </div>
</section>

{# 5. COMMUNITY CTA #}
<section class="community-cta">
  <div class="container text-center">
    <h2>Entra nel <span class="highlight">Cerchio</span></h2>
    <p>Segui i nostri aggiornamenti live e le storie dai campi su Instagram.</p>
    <div class="social-links">
      <a href="#" class="social-btn ig">Instagram</a>
      <a href="#" class="social-btn telegram">Telegram Risultati</a>
    </div>
  </div>
</section>